{# _navbar.html.twig #}

<nav class="navbar navbar-expand-lg bg-primary d-flex px-5">
    <div class="container-fluid">
        <div>
            <a class="navbar-brand link-light d-flex" href="/">
                <img src="{{ asset('build/images/tds_logo.png') }}" width="70px" alt="" id="logo">
                <h1 class="ps-4">Trackers de Saveurs</h1>
            </a>
            {% if app.user %}
                <a href="{{ path('app_favoris') }}">
                    <button type="button" class="btn btn-secondary me-2">Mon espace</button>
                </a>
            {% endif %}
            {% if app.user is null %}
                <a href="{{ path('app_login') }}">
                    <button type="button" class="btn btn-light">Connexion</button>
                </a>
            {% endif %}

            {% if app.user %}
                <a href="{{ path('app_logout') }}">
                    <button type="button" class="btn btn-danger">Déconnexion</button>
                </a>
            {% endif %}
        </div>
    </div>
</nav>
