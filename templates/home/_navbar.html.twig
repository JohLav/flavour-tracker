<nav class="navbar navbar-expand-lg sticky-top bg-transparent justify-content-between align-items-center px-5">
        <a class="d-flex navbar-brand link-light align-items-center mx-auto mx-md-0" href="/">
		    <img src="{{ asset('build/images/tds_logo.png') }}" width="35px" alt="" id="logo">
            <h1 class="ps-3 blockquote mb-0">Tracker de Saveurs</h1>
        </a>
	<div class="d-flex gap-2 mx-auto mx-md-0 mb-3 mb-md-0">
		{% if app.user %}
			<a href="{{ path('user_show', {'id': app.user.id}) }}">
				<button class="btn btn-light" style="min-width: 125px;" type="button">Profil</button>
			</a>
			<a href="{{ path('logout') }}">
				<button class="btn btn-danger" style="min-width: 125px;" type="button">Déconnexion</button>
			</a>
		{% elseif app.user is null %}
			<a href="{{ path('login') }}">
				<button class="btn btn-light" style="min-width: 125px;" type="button">Connexion</button>
			</a>
		{% endif %}
	</div>
</nav>
