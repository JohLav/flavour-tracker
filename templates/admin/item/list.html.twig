{% extends 'admin/admin.base.html.twig' %}

{% block title %}Liste des éléments{% endblock %}

{% block content %}

    <div class="col-9 mt-5">
        <h2>Liste des éléments</h2>
        <div class="d-flex ">
            {% for item in items %}
                {% include 'admin/item/_card.html.twig' %}
            {% else %}
                <div>
                    Il n'y a pas encore de menus.
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}
